# 热门推荐显示旧数据修复方案

## 问题分析

页面底部的"热门推荐"区域显示旧的菜谱名称和类型，原因是：
- 使用了 `autoSort` 接口（旧接口）
- 没有使用新的智能推荐系统

## 解决方案

### 方案1：使用智能推荐的热门模式（推荐）

**优点**：
- 使用统一的推荐系统
- 自动过滤用户过敏原
- 有缓存机制，性能更好
- 数据一致性好

**修改步骤**：

#### 1. 修改 list.vue 的 getHotList 方法

```javascript
// 原代码（第178-192行）
getHotList() {
  let autoSortUrl = "";
  autoSortUrl = "caipuxinxi/autoSort";
  if(localStorage.getItem('Token')) {
      autoSortUrl = "caipuxinxi/autoSort2";
  }
    this.$http.get(autoSortUrl, {params: {
        page: 1,
        limit: 4,
    }}).then(res => {
        if (res.data.code == 0) {
            this.hotList = res.data.data.list;
        }
    })
},

// 修改为：
getHotList() {
  // 如果用户已登录，使用智能推荐的热门模式
  if(localStorage.getItem('Token') && localStorage.getItem('userid')) {
    this.$http.get('caipuxinxi/recommend', {
      params: {
        userId: localStorage.getItem('userid'),
        pageNum: 1,
        pageSize: 4,
        recommendType: 'hot',
        sortType: 'popularity'
      }
    }).then(res => {
      if (res.data.code == 0) {
        this.hotList = res.data.data.list;
      }
    }).catch(err => {
      // 如果推荐接口失败，降级使用旧接口
      console.warn('热门推荐接口失败，使用降级方案', err);
      this.getHotListFallback();
    });
  } else {
    // 未登录用户使用旧接口
    this.getHotListFallback();
  }
},

// 添加降级方法
getHotListFallback() {
  let autoSortUrl = "caipuxinxi/autoSort";
  if(localStorage.getItem('Token')) {
    autoSortUrl = "caipuxinxi/autoSort2";
  }
  this.$http.get(autoSortUrl, {
    params: {
      page: 1,
      limit: 4,
    }
  }).then(res => {
    if (res.data.code == 0) {
      this.hotList = res.data.data.list;
    }
  });
},
```

#### 2. 修改 detail.vue 的 getHotList 方法

同样的修改应用到 `detail.vue` 文件（第209-223行）。

---

### 方案2：清除数据库缓存并重新导入数据

**优点**：
- 保持原有接口不变
- 简单直接

**缺点**：
- 需要重新导入数据
- 不能利用新的推荐系统

**修改步骤**：

#### 1. 清除Redis缓存

```bash
# 连接Redis
redis-cli

# 清除所有缓存
FLUSHALL

# 或者只清除热门推荐缓存
KEYS recommend:hot:*
DEL recommend:hot:page1:size4
```

#### 2. 更新数据库数据

```sql
-- 更新菜谱名称和类型为新数据
UPDATE caipuxinxi SET 
  caipumingcheng = '新菜谱名称',
  caishileixing = '新菜式类型'
WHERE id = 31;

-- 批量更新示例
UPDATE caipuxinxi SET 
  caipumingcheng = CASE id
    WHEN 31 THEN '鸡腿肉沙拉'
    WHEN 32 THEN '土豆炖牛肉'
    WHEN 33 THEN '清炒白菜'
    ELSE caipumingcheng
  END,
  caishileixing = CASE id
    WHEN 31 THEN '健身餐'
    WHEN 32 THEN '家常菜'
    WHEN 33 THEN '素菜'
    ELSE caishileixing
  END
WHERE id IN (31, 32, 33);
```

#### 3. 重启后端服务

```bash
# 停止服务
# 重新启动
mvn spring-boot:run
```

---

### 方案3：混合方案（最佳）

结合方案1和方案2：
1. 先更新数据库数据（确保数据正确）
2. 再修改前端代码（使用新推荐系统）
3. 清除缓存（让新数据生效）

---

## 推荐实施步骤

### 第一步：更新数据库（如果需要）

如果数据库中的菜谱信息是旧的，先更新数据库。

### 第二步：修改前端代码

使用方案1的代码修改 `list.vue` 和 `detail.vue`。

### 第三步：清除缓存

```bash
redis-cli
FLUSHALL
```

### 第四步：重启服务

```bash
# 停止当前服务
# 重新启动
mvn clean spring-boot:run
```

### 第五步：测试

1. 访问菜谱列表页面
2. 检查底部"热门推荐"区域
3. 确认显示的是新数据

---

## 验证方法

### 1. 检查API响应

打开浏览器开发者工具 → Network → 查看请求：

**旧接口**：
```
GET /springbootct3p7/caipuxinxi/autoSort?page=1&limit=4
```

**新接口**：
```
GET /springbootct3p7/caipuxinxi/recommend?userId=11&pageNum=1&pageSize=4&recommendType=hot&sortType=popularity
```

### 2. 检查返回数据

新接口返回的数据结构：
```json
{
  "code": 0,
  "data": {
    "list": [
      {
        "id": 31,
        "caipumingcheng": "新菜谱名称",
        "caishileixing": "新菜式类型",
        "fenshu": 92,
        ...
      }
    ],
    "total": 4
  }
}
```

---

## 注意事项

1. **兼容性**：方案1保留了降级机制，即使新接口失败也能正常显示
2. **缓存**：热门推荐有6小时缓存，修改后需要等待或手动清除缓存
3. **用户体验**：未登录用户仍使用旧接口，不影响访客浏览
4. **数据一致性**：建议同时更新数据库和修改代码，确保数据一致

---

## 快速修复命令

如果您想快速修复，执行以下步骤：

1. 清除Redis缓存：
```bash
redis-cli FLUSHALL
```

2. 重启后端：
```bash
# 在项目根目录
mvn clean spring-boot:run
```

3. 刷新浏览器：
```
Ctrl + F5
```

如果问题仍然存在，说明是数据库数据问题，需要更新数据库。

# 智能推荐算法详解

## 概述

系统提供了四种推荐模式，每种都有不同的推荐策略和应用场景：

1. **📦 基于库存推荐** (stock_based) - 默认模式
2. **🔥 热门推荐** (hot)
3. **💝 个性化推荐** (personalized)
4. **📋 普通浏览** (normal)

---

## 1. 📦 基于库存推荐 (Stock-Based)

### 核心思想
根据用户当前拥有的食材，推荐最匹配的菜谱，帮助用户充分利用现有食材。

### 算法流程

```
1. 获取用户有效食材
   ↓
2. 获取用户过敏信息和健康偏好
   ↓
3. 获取所有食谱
   ↓
4. 对每个食谱：
   - 过滤包含过敏原的食谱
   - 计算食材匹配度
   - 检查健康偏好匹配
   - 计算热门权重
   - 计算综合分数
   ↓
5. 按综合分数排序
   ↓
6. 返回分页结果
```

### 关键计算公式

#### 1) 食材匹配度
```java
匹配度 = (用户拥有的食材数 / 食谱所需食材总数) × 100%

示例：
食谱需要：西红柿、鸡蛋、盐、糖 (4种)
用户拥有：西红柿、鸡蛋、盐 (3种)
匹配度 = 3/4 × 100% = 75%
```

#### 2) 热门权重
```java
热门权重 = (收藏量×0.4 + 评分×10×0.3 + 点击量/100×0.3) / 100 + 1.0
范围：1.0 - 2.0

示例：
收藏量 = 50
评分 = 85
点击量 = 200
热门权重 = (50×0.4 + 85×10×0.3 + 200/100×0.3) / 100 + 1.0
        = (20 + 255 + 0.6) / 100 + 1.0
        = 2.756 / 100 + 1.0
        = 1.028
```

#### 3) 综合分数
```java
综合分数 = 匹配度 × 热门权重 × 健康偏好加成

健康偏好加成：
- 匹配用户健康偏好：×1.2 (加成20%)
- 不匹配：×1.0 (无加成)

示例：
匹配度 = 75%
热门权重 = 1.028
健康偏好匹配 = 是
综合分数 = 75 × 1.028 × 1.2 = 92.52
```

### 特色功能

1. **过敏原过滤**：自动排除包含用户过敏原的食谱
2. **健康偏好匹配**：
   - 素食 (vegetarian)：优先推荐含"素"、"蔬菜"的菜谱
   - 低脂 (low_fat)：优先推荐"清淡"、"蒸"类菜谱
   - 高蛋白 (high_protein)：优先推荐含"肉"、"蛋"的菜谱
3. **去重机制**：排除已收藏和近期浏览的食谱

### 适用场景
- 用户想用现有食材做菜
- 减少食材浪费
- 快速决策今天吃什么

---

## 2. 🔥 热门推荐 (Hot)

### 核心思想
推荐当前最受欢迎的菜谱，基于大众口味和流行趋势。

### 算法流程

```
1. 检查热门食谱缓存（6小时有效期）
   ↓
2. 如果缓存命中，直接返回
   ↓
3. 如果缓存未命中：
   - 获取所有食谱
   - 过滤用户过敏原
   - 计算热门分数
   ↓
4. 按热门分数排序
   ↓
5. 缓存结果（6小时）
   ↓
6. 返回分页结果
```

### 热门分数计算

```java
热门分数 = 收藏量×0.4 + 评分×10×0.3 + 点击量/100×0.3

权重分配：
- 收藏量：40% (最重要，代表用户真实喜好)
- 评分：30% (代表菜谱质量)
- 点击量：30% (代表关注度)

示例：
菜谱A：收藏100次，评分90，点击500次
热门分数 = 100×0.4 + 90×10×0.3 + 500/100×0.3
        = 40 + 270 + 1.5
        = 311.5

菜谱B：收藏50次，评分95，点击1000次
热门分数 = 50×0.4 + 95×10×0.3 + 1000/100×0.3
        = 20 + 285 + 3
        = 308

结果：菜谱A排名更高
```

### 特色功能

1. **缓存机制**：热门榜单缓存6小时，减少数据库查询
2. **过敏原过滤**：仍然会过滤用户过敏的食谱
3. **实时更新**：每6小时自动更新热门榜单

### 适用场景
- 新用户不知道做什么
- 想尝试流行菜谱
- 寻找高评分菜谱

---

## 3. 💝 个性化推荐 (Personalized)

### 核心思想
基于协同过滤算法，推荐"和你相似的用户喜欢的菜谱"。

### 算法流程

```
1. 获取用户食材库存
   ↓
2. 查找相似用户（拥有相似食材的用户）
   ↓
3. 获取相似用户收藏的食谱
   ↓
4. 排除当前用户已收藏的食谱
   ↓
5. 过滤过敏原
   ↓
6. 计算匹配度和热门权重
   ↓
7. 排序并返回
```

### 相似用户查找算法

使用 **Jaccard 相似度** 计算用户之间的相似程度：

```java
Jaccard相似度 = |A ∩ B| / |A ∪ B|

其中：
A = 当前用户的食材集合
B = 其他用户的食材集合
∩ = 交集（共同拥有的食材）
∪ = 并集（所有食材）

示例：
用户A拥有：{西红柿, 鸡蛋, 盐, 糖, 葱}
用户B拥有：{西红柿, 鸡蛋, 盐, 酱油, 醋}

交集 = {西红柿, 鸡蛋, 盐} = 3个
并集 = {西红柿, 鸡蛋, 盐, 糖, 葱, 酱油, 醋} = 7个

相似度 = 3/7 = 0.43 (43%)
```

### 相似度阈值
- 相似度 > 30% 才认为是相似用户
- 取相似度最高的前10个用户

### 推荐逻辑

```
假设：
- 用户A（你）拥有：西红柿、鸡蛋、盐
- 用户B（相似用户）拥有：西红柿、鸡蛋、盐、酱油
- 用户B收藏了：西红柿炒鸡蛋、番茄蛋汤

推荐给用户A：
1. 西红柿炒鸡蛋（如果A还没收藏）
2. 番茄蛋汤（如果A还没收藏）

理由：拥有相似食材的用户B喜欢这些菜谱，
     所以A可能也会喜欢
```

### 降级策略

如果找不到相似用户或相似用户没有收藏，自动降级为"基于库存推荐"。

### 适用场景
- 想发现新菜谱
- 寻找灵感
- 探索其他人的做法

---

## 4. 📋 普通浏览 (Normal)

### 核心思想
传统的列表浏览模式，不使用任何推荐算法。

### 特点

1. **按评分排序**：默认按菜谱评分从高到低排序
2. **支持搜索**：可以按菜谱名称、菜式类型、烹饪方式搜索
3. **无个性化**：所有用户看到的结果相同
4. **无缓存**：实时查询数据库

### 适用场景
- 用户想自己浏览所有菜谱
- 明确知道要找什么菜
- 不需要推荐功能

---

## 推荐算法对比

| 特性 | 基于库存 | 热门推荐 | 个性化 | 普通浏览 |
|------|---------|---------|--------|---------|
| **核心依据** | 用户食材 | 大众喜好 | 相似用户 | 无 |
| **个性化程度** | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐ | ⭐ |
| **计算复杂度** | 中 | 低 | 高 | 低 |
| **缓存策略** | 1小时 | 6小时 | 1小时 | 无 |
| **过敏原过滤** | ✅ | ✅ | ✅ | ❌ |
| **健康偏好** | ✅ | ❌ | ❌ | ❌ |
| **去重机制** | ✅ | ❌ | ✅ | ❌ |
| **适合新用户** | ❌ | ✅ | ❌ | ✅ |
| **适合老用户** | ✅ | ⭐⭐⭐ | ✅ | ⭐⭐ |

---

## 缓存策略

### 为什么需要缓存？

推荐算法涉及大量计算：
- 基于库存：需要计算每个食谱的匹配度
- 热门推荐：需要统计收藏、评分、点击量
- 个性化：需要查找相似用户并计算相似度

缓存可以显著提升性能。

### 缓存键设计

```java
// 基于库存推荐缓存键
recommend:user:{userId}:stock_based:score:page{pageNum}:size{pageSize}

// 热门推荐缓存键
recommend:hot:page{pageNum}:size{pageSize}

// 个性化推荐缓存键
recommend:user:{userId}:personalized:score:page{pageNum}:size{pageSize}
```

### 缓存时效

- **基于库存/个性化**：1小时
  - 原因：用户食材可能变化，需要较快更新
- **热门推荐**：6小时
  - 原因：热门榜单变化较慢，可以缓存更久

### 缓存刷新

用户可以点击"刷新推荐"按钮强制刷新缓存。

---

## 实际应用示例

### 场景1：周末想做菜

**用户情况**：
- 冰箱有：西红柿、鸡蛋、土豆、牛肉
- 过敏：海鲜
- 健康偏好：无

**推荐模式选择**：📦 基于库存

**推荐结果**：
1. 西红柿炒鸡蛋 (匹配度100%)
2. 土豆炖牛肉 (匹配度100%)
3. 番茄牛肉 (匹配度75% - 缺少洋葱)

### 场景2：新用户不知道做什么

**用户情况**：
- 刚注册，没有食材记录
- 想看看大家都在做什么

**推荐模式选择**：🔥 热门推荐

**推荐结果**：
1. 宫保鸡丁 (收藏500次，评分95)
2. 红烧肉 (收藏450次，评分92)
3. 麻婆豆腐 (收藏400次，评分90)

### 场景3：想尝试新菜谱

**用户情况**：
- 经常做家常菜
- 想发现新的菜谱

**推荐模式选择**：💝 个性化

**推荐逻辑**：
1. 找到和你食材相似的用户
2. 看看他们收藏了什么
3. 推荐你还没试过的菜谱

---

## 技术亮点

### 1. 多维度评分系统
不仅考虑匹配度，还综合了热门度、健康偏好等多个维度。

### 2. 智能过滤
自动过滤过敏原和已收藏/浏览过的食谱，避免重复推荐。

### 3. 协同过滤
使用Jaccard相似度算法找到相似用户，实现真正的个性化推荐。

### 4. 分层缓存
不同推荐模式使用不同的缓存策略，平衡性能和实时性。

### 5. 降级策略
当某种推荐模式无法工作时，自动降级到其他模式，保证用户体验。

---

## 未来优化方向

### 1. 深度学习推荐
使用神经网络学习用户偏好，提供更精准的推荐。

### 2. 实时推荐
基于用户当前行为（浏览、搜索）实时调整推荐结果。

### 3. 多目标优化
同时优化多个目标：匹配度、新颖性、多样性。

### 4. A/B测试
对比不同推荐算法的效果，持续优化。

### 5. 社交推荐
引入好友关系，推荐好友喜欢的菜谱。

---

## 总结

四种推荐模式各有特色，满足不同场景需求：

- **基于库存**：最实用，帮助用户充分利用现有食材
- **热门推荐**：最简单，适合新用户快速上手
- **个性化**：最智能，发现新菜谱的好帮手
- **普通浏览**：最传统，给用户完全的控制权

系统默认使用"基于库存"推荐，因为它最符合智能厨房管理的核心价值：**减少食材浪费，提高食材利用率**。
